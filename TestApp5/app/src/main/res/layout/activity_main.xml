<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:gravity="center"
    tools:context=".MainActivity"
    android:orientation="vertical"
    android:padding="16dp">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="2*. Расположить на экране два контейнера на каждом по три текстовых поля для ввода цифр, на первом контейнере есть еще и три кнопки. После ввода цифр в десятичном формате и нажатии соответствующей кнопки в первом контейнере - во втором контейнере соответствующее поле заполняется тем же числом, но в другой системе счисления (двоичной, восьмеричной и шестнадцатеричной соответственно).
2.1** Реализовать тоже задание, но без кнопок. Цифры преобразуются автоматически, каждый раз, как только пользователь вводит новые данные (изменения отслеживаются с помощью TextWatcher)."/>

    <FrameLayout
        android:layout_width="180dp"
        android:layout_height="250dp"
        android:layout_gravity="center|left"
        android:id="@+id/container"/>

    <FrameLayout
        android:id="@+id/container1"
        android:layout_width="198dp"
        android:layout_height="250dp"
        android:layout_gravity="center|right" />
</FrameLayout>